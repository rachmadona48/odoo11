<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="currency_rate.list">
      <field name="name">currency_rate list</field>
      <field name="model">currency_rate.currency_rate</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="currency_rate.action_window">
      <field name="name">currency_rate window</field>
      <field name="res_model">currency_rate.currency_rate</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="currency_rate.action_server">
      <field name="name">currency_rate server</field>
      <field name="model_id" ref="model_currency_rate_currency_rate"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="currency_rate" id="currency_rate.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="currency_rate.menu_1" parent="currency_rate.menu_root"/>
    <menuitem name="Menu 2" id="currency_rate.menu_2" parent="currency_rate.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="currency_rate.menu_1_list" parent="currency_rate.menu_1"
              action="currency_rate.action_window"/>
    <menuitem name="Server to list" id="currency_rate" parent="currency_rate.menu_2"
              action="currency_rate.action_server"/>
    -->
  </data>
</odoo>